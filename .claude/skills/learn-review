---
name: learn-review
description: 交互式学习复习系统 — 支持学习新模式、复习旧知识，持续提问直到用户退出
---

<SUBAGENT-STOP>
如果你是被子代理派来执行特定任务的，跳过此 skill。
</SUBAGENT-STOP>

# learn-review — 学习复习系统

## 触发方式

用户说以下关键词之一：
- `/learn-review`
- "开始学习"、"开始复习"、"巩固"、"来一轮"

## 启动

1. 读取 `learning-track.md` 文件
2. 询问用户选择模式：
   - **1-学习模式**：选 score=0 的知识点，讲解+提问
   - **2-复习模式**：选 score>0 的知识点，提问巩固
   - **3-混合模式**：先复习低分项，再学新知识
3. 进入循环

---

## learning-track.md 格式

每条记录格式如下：

```markdown
### 文件名.md — 显示名称
- path: 相对项目根目录的路径
- score: 掌握度 0~100
- lastAsked: 上次提问日期 YYYY-MM-DD
- lastUpdated: 上次分数更新日期 YYYY-MM-DD
- askedCount: 提问次数
- notes: |
    已掌握: xxx
    待巩固: xxx
```

---

## 衰减规则（每次启动时执行）

遍历所有知识点：
```
如果 lastUpdated 不是 "-"：
    天数差 = today - lastUpdated（日期差）
    score = max(0, score - 天数差 × 1)
    更新 lastUpdated = today
```

---

## 提问优先级公式

```
优先级 = (100 - score) × 0.5 + 距离天数 × 0.5
```

其中距离天数 = today - lastAsked（如果 lastAsked 为 "-"，按 30 天算）

按优先级从高到低排序，每次选前 3 个。

---

## 打分规则

根据用户回答质量调整 score：

| 回答质量 | 调整 | 标准 |
|----------|------|------|
| 完美 | +15 | 能解释原理，能举例说明 |
| 基本正确 | +10 | 答出核心要点 |
| 部分正确 | +5 | 有遗漏或不准确 |
| 答错 | -15 | 不知道或理解错误 |

调整后：
- score = max(0, min(100, score))
- 更新 lastUpdated = today
- 更新 askedCount += 1
- 更新 lastAsked = today

---

## 学习模式流程

1. 从所有 score=0 的知识点中，按添加顺序选一个
2. 读取 `path` 指向的文件，提取核心概念
3. 向用户简要讲解（不是逐字读，提炼要点）
4. 提 1~2 个问题确认理解
5. 根据回答打分
6. 更新 notes，记录已掌握和待巩固的内容
7. 询问："继续学习下一个 / 切换到复习模式 / 退出？"

## 复习模式流程

1. 执行衰减
2. 按优先级选前 3 个知识点
3. 对每个知识点：
   a. 先看 notes 中的"待巩固"项，针对薄弱点提问
   b. 如果没有待巩固项，从文档中选 1 个核心概念提问
   c. 根据回答打分
   d. 更新 notes
4. 询问："继续复习 / 切换到学习模式 / 退出？"

## 混合模式流程

1. 先执行复习模式 1 轮（3 个知识点）
2. 再执行学习模式 1 轮（1 个新知识点）
3. 询问："继续 / 退出？"

---

## 退出

用户说"退出"、"结束"、"停"、"不学了" 时：
1. 输出本次学习摘要：
   - 学习了/复习了几个知识点
   - 分数变化
   - 建议下次重点
2. 更新 learning-track.md 文件
3. 结束
